package com.fendrixx.aurus.api.menu;

public enum AreaType {
  /** Area whose components never move or change. Rendered once. */
  STATIC,

  /** Area that supports vertical scrolling of its components. */
  SCROLL;
}